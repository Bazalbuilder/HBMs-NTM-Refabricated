package com.bazalbuilder.hbm.config;

/**
 * now's not the time for a JSON-based configuration, so this'll be just constant fields for now
 * @author bazal
 */
public class TemporaryConstants {
	public static final int FAT_MAN_MAX_RADIUS = 175;
}
